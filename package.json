{
  "name": "medical-service-center",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && yarn dev",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false yarn add --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "express": "^4.17.2",
    "jquery": "^3.6.0",
    "socket.io": "^4.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
